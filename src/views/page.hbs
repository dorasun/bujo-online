{{#if page}}
	<h1>{{page.title}}</h1>
	<p> THIS FORM DOESN'T WORK YET AHH </p>
	<ul>
		{{#each page.items}}
			{{#each this.tasks}}
				<li>{{this.content}}</li>
			{{/each}}

			{{#each this.events}}
				<li>{{this.content}}</li>
			{{/each}}

			{{#each this.notes}}
				<li>{{this.content}}</li>
			{{/each}}
		{{/each}}

		<li>
			<form method="POST" action="{{page.slug}}-add">
				<select name="type">
					<option value="note">Note</option>
					<option value="task">Task</option>
					<option value="event">Event</option>
				</select>
				<input type = "text" name = "content" placeholder = "Content"> 

				<input type = "submit" value = "Add New Item">
			</form>
		</li>
	</ul>
{{/if}}